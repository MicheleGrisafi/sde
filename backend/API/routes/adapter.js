/* API for the note adapter object */
//TODO: use page limits
const send_error = require("../../modules/send_error");

module.exports = (app, db) => {
    app.get("/API/adapter/onenote",(req,res)=>{});

    app.get("/API/adapter/evernote", (req,res)=>{});



}

